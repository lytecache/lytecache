#!/bin/sh
DIRNAME=$(dirname "$0")
APP_HOME=$(cd "$DIRNAME" >/dev/null && pwd)

if [ -z "$JAVA_HOME" ]; then
    JAVA_BIN=java
else
    JAVA_BIN="$JAVA_HOME/bin/java"
fi

exec "$JAVA_BIN" -Xmx64m -Xms64m -cp "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain "$@"
